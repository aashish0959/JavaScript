#include<stdio.h>
int main(){
int arr[5], sum=0,size;
printf("Enter the Number :");
scanf("%d",&arr[0]);
for (int i = 0; i < size; i++)
{
    sum += arr[i];
}
printf(" Fibonacci of %d is : %d ",arr[0] ,sum);
    return 0 ;
}

